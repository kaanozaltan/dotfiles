# Kaan's dotfiles

Personal dotfiles repository. Run setup.sh to symlink configs under `$HOME` to the ones located here.

```sh
bash setup.sh
```

## Local git config
Add any local settings, such as git identity, in `~/.gitconfig.local` to be imported by `.gitconfig`.
